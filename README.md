# 🤖 Семейный бюджет бот для Telegram

Простой и удобный бот для учета расходов двух пользователей с автоматическим определением категорий.

## 📋 Возможности

- ✅ Добавление расходов простым сообщением: "500 продукты"
- 🎯 Автоматическое определение категорий по ключевым словам
- 📊 Статистика по категориям и пользователям
- 💰 Расчет баланса - кто кому должен
- 📝 История трат с возможностью удаления
- 🌍 Работает 24/7 на бесплатном хостинге

## 🚀 Быстрый старт

### Шаг 1: Создание бота в Telegram

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Придумайте имя для бота (например: "Family Budget")
4. Придумайте username (должен заканчиваться на `bot`, например: `my_family_budget_bot`)
5. **Сохраните токен**, который вам выдаст BotFather (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Шаг 2: Узнайте ваш Telegram ID

1. Найдите в Telegram бота [@userinfobot](https://t.me/userinfobot)
2. Отправьте ему любое сообщение
3. Он пришлет вам ваш ID (число вроде `123456789`)
4. Попросите вашу жену сделать то же самое

### Шаг 3A: Запуск на компьютере (для тестирования)

**Требования:**
- Python 3.9 или выше
- Windows, macOS или Linux

**Установка:**

```bash
# 1. Скачайте файлы бота (все файлы из этой папки)

# 2. Откройте терминал/командную строку в папке с ботом

# 3. Создайте виртуальное окружение
python -m venv venv

# 4. Активируйте его
# Windows:
venv\Scripts\activate
# Mac/Linux:
source venv/bin/activate

# 5. Установите зависимости
pip install -r requirements.txt

# 6. Создайте файл .env и добавьте токен
# Windows:
copy .env.example .env
# Mac/Linux:
cp .env.example .env

# 7. Откройте .env в любом текстовом редакторе и замените:
TELEGRAM_BOT_TOKEN=ваш_токен_от_botfather

# 8. Запустите бота
python bot.py
```

Готово! Бот работает на вашем компьютере (пока он включен).

### Шаг 3B: Запуск на бесплатном хостинге Render.com (рекомендуется)

Это лучший вариант - бот будет работать 24/7 бесплатно!

**1. Подготовка:**

Зарегистрируйтесь на [GitHub.com](https://github.com) (если нет аккаунта)

**2. Загрузка кода на GitHub:**

```bash
# Если у вас установлен Git:
cd family_budget_bot
git init
git add .
git commit -m "Initial commit"

# Создайте новый репозиторий на GitHub и выполните:
git remote add origin https://github.com/ваш-username/family-budget-bot.git
git push -u origin main
```

Если Git не установлен, можно просто:
- Зайти на GitHub.com
- Создать новый репозиторий
- Загрузить все файлы через веб-интерфейс

**3. Деплой на Render:**

1. Зайдите на [Render.com](https://render.com) и зарегистрируйтесь (можно через GitHub)
2. Нажмите **"New +"** → **"Web Service"**
3. Подключите ваш GitHub репозиторий
4. Настройки:
   - **Name**: `family-budget-bot` (или любое имя)
   - **Environment**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python bot.py`
   - **Instance Type**: `Free`
5. В разделе **Environment Variables** добавьте:
   - Key: `TELEGRAM_BOT_TOKEN`
   - Value: ваш токен от BotFather
6. Нажмите **"Create Web Service"**

Готово! Через 2-3 минуты бот запустится и будет работать 24/7 бесплатно! 🎉

**⚠️ Важно для бесплатного плана Render:**
- Сервис "засыпает" после 15 минут неактивности
- Первое сообщение может обрабатываться медленнее (пока бот "просыпается")
- Это нормально и не мешает использованию!

## 📱 Использование бота

### Первый запуск

1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Попросите жену сделать то же самое
4. Готово! Бот будет работать только для вас двоих

### Основные команды

**Добавить расход:**
Просто напишите сумму и описание:
```
500 ашан
1200 такси
350 кофе
2500 икея
```

Бот автоматически определит категорию!

**Команды:**
- `/stats` - посмотреть статистику
- `/balance` - баланс между вами (кто кому должен)
- `/history` - последние траты
- `/categories` - список категорий и ключевых слов
- `/delete [ID]` - удалить расход

### Категории

Бот автоматически распознает следующие категории:

🍔 **Еда**: продукты, еда, ашан, лидл, магнит, пятерочка и т.д.
🚗 **Транспорт**: такси, бензин, метро, автобус и т.д.
🎉 **Развлечения**: кино, ресторан, кафе, бар и т.д.
💊 **Здоровье**: аптека, врач, лекарства и т.д.
🏠 **Дом**: квартира, коммуналка, ремонт, мебель и т.д.
👕 **Одежда**: одежда, обувь, zara, h&m и т.д.
📱 **Связь**: телефон, интернет, тариф и т.д.
📦 **Прочее**: все остальное

## 🔧 Настройка

### Добавление своих категорий

Откройте файл `categories.py` и добавьте свои ключевые слова в словарь `CATEGORIES`:

```python
CATEGORIES = {
    'Еда': [
        'продукты', 'еда', 'ашан', 'лидл',
        'ваш_магазин',  # добавьте сюда
    ],
    # ...
}
```

### Ограничение доступа

По умолчанию бот разрешает доступ первым двум пользователям, кто напишет `/start`.

Если хотите явно указать ID пользователей, откройте `bot.py` и замените:

```python
ALLOWED_USERS = []  # Оставьте пустым
```

на:

```python
ALLOWED_USERS = [123456789, 987654321]  # Ваши Telegram ID
```

## 💾 Хранение данных

Данные хранятся в SQLite базе данных (`expenses.db`). 

**На Render.com**: База данных создается автоматически, но при перезапуске сервиса может сброситься. Для постоянного хранения можно подключить PostgreSQL (инструкции доступны в документации Render).

**Экспорт данных**: Скачайте файл `expenses.db` - это обычная SQLite база, которую можно открыть любым SQL-клиентом.

## 🆘 Частые вопросы

**Q: Бот не отвечает**
- Проверьте, что токен правильный
- Проверьте логи на Render (вкладка "Logs")
- Убедитесь, что сервис запущен

**Q: Как добавить третьего пользователя?**
- Измените код в `bot.py`, строка `if len(ALLOWED_USERS) < 2:`
- Замените `2` на нужное количество

**Q: Можно ли изменить валюту?**
- Да, в `bot.py` замените все `₽` на нужный символ (€, $, zł)

**Q: Бот медленно отвечает на Render**
- Это нормально для бесплатного плана - сервис "просыпается" после неактивности
- Для моментальных ответов нужен платный план ($7/мес)

## 📝 Лицензия

Этот проект создан для личного использования. Свободно модифицируйте под свои нужды!

## 🙏 Поддержка

Если что-то не работает, проверьте:
1. Правильность токена в `.env` или на Render
2. Логи ошибок (в консоли или на Render)
3. Версию Python (должна быть 3.9+)

Удачи! 🚀
